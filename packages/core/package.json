{
  "name": "@pgsts/core",
  "private": true,
  "version": "0.1.0",
  "description": "Domain rules. No next/*, no @prisma/client imports — depends on ports (interfaces) only.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}
